% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathwise_sensitivities.R
\name{pathwise_vega}
\alias{pathwise_vega}
\title{Pathwise Vega Estimator for Geometric Brownian Motion}
\usage{
pathwise_vega(
  paths,
  strikes,
  maturity = max(paths$time, na.rm = TRUE),
  risk_free_rate,
  option_type = c("call", "put"),
  volatility,
  initial_price = NULL
)
}
\arguments{
\item{paths}{Tibble of simulated paths produced by \code{simulate_paths()} for a
GBM specification containing columns \code{path_id}, \code{time}, and \code{stock_price}.}

\item{strikes}{Numeric vector of strike prices.}

\item{maturity}{Numeric time to maturity in years. Defaults to the maximum
\code{time} present in \code{paths}.}

\item{risk_free_rate}{Numeric continuously compounded risk-free rate.}

\item{option_type}{Character string, one of "call" or "put".}

\item{volatility}{Numeric volatility used in the simulation.}

\item{initial_price}{Optional numeric initial stock price. When omitted the
value is retrieved from the path specification attribute.}
}
\value{
Tibble with columns \code{strike} and \code{vega_pathwise}.
}
\description{
Computes the Monte Carlo pathwise estimator of the option vega for
geometric Brownian motion simulations.
}
\details{
Strikes are evaluated sequentially. Use your preferred parallel backend to
distribute the work if lower latency is required.
}
\examples{
spec <- gbm_spec(initial_value = 100, drift = 0.05, volatility = 0.2)
paths <- simulate_paths(spec, n_paths = 5000, n_steps = 128, maturity = 1, seed = 42)
pathwise_vega(paths, strikes = 100, risk_free_rate = 0.05, volatility = 0.2)

}
