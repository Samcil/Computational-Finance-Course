% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heston_model.R
\name{heston_spec}
\alias{heston_spec}
\title{Create Heston Model Specification}
\usage{
heston_spec(
  initial_price,
  initial_variance,
  risk_free_rate,
  dividend_yield = 0,
  mean_reversion,
  long_term_variance,
  vol_of_vol,
  correlation,
  scheme = c("euler", "aes")
)
}
\arguments{
\item{initial_price}{Numeric. Initial asset price \eqn{S_0 > 0}.}

\item{initial_variance}{Numeric. Initial variance \eqn{v_0 \ge 0}.}

\item{risk_free_rate}{Numeric. Continuously compounded risk-free rate \eqn{r}.}

\item{dividend_yield}{Numeric. Continuous dividend yield \eqn{q}. Default is 0.}

\item{mean_reversion}{Numeric. Mean reversion speed \eqn{\kappa > 0}.}

\item{long_term_variance}{Numeric. Long-term variance level \eqn{\theta > 0}.}

\item{vol_of_vol}{Numeric. Volatility of variance \eqn{\xi > 0}.}

\item{correlation}{Numeric. Instantaneous correlation \eqn{\rho \in [-1, 1]} between
the Brownian motions driving price and variance.}

\item{scheme}{Character. Simulation scheme, either \code{"euler"} for Euler-Maruyama
or \code{"aes"} for the Andersen quadratic exponential scheme.}
}
\value{
An object of class \code{heston_spec} inheriting from \code{process_spec}.
}
\description{
Defines the stochastic volatility dynamics introduced by Heston (1993).
The specification integrates with \code{simulate_paths()} to generate joint stock
price and variance trajectories in a pipe-friendly workflow.
}
\details{
The Heston model evolves according to:
\deqn{\begin{aligned}
  dS(t) &= (r - q) S(t) dt + \sqrt{v(t)} S(t) dW_S(t), \\
  dv(t) &= \kappa (\theta - v(t)) dt + \xi \sqrt{v(t)} dW_v(t),
\end{aligned}}
with correlation \eqn{dW_S(t) dW_v(t) = \rho dt}.

\strong{Feller Condition}: \eqn{2\kappa\theta \ge \xi^2} prevents the variance from
hitting zero. When the condition is violated the Euler scheme still runs but
the variance is reflected at zero during simulation.
}
\examples{
spec <- heston_spec(
  initial_price = 100,
  initial_variance = 0.04,
  risk_free_rate = 0.02,
  dividend_yield = 0,
  mean_reversion = 1.5,
  long_term_variance = 0.04,
  vol_of_vol = 0.5,
  correlation = -0.7
)

spec |>
  simulate_paths(n_paths = 100, n_steps = 252, maturity = 1)

}
