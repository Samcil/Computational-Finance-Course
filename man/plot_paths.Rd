% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_paths.R
\name{plot_paths}
\alias{plot_paths}
\title{Plot Simulated Paths}
\usage{
plot_paths(
  paths_data,
  n_paths_plot = NULL,
  plot_type = c("standard", "compensated"),
  alpha = 0.3,
  theme = c("minimal", "classic", "bw")
)
}
\arguments{
\item{paths_data}{Tibble. Output from \code{simulate_paths()}.}

\item{n_paths_plot}{Integer. Maximum number of paths to plot. Default is NULL (all paths).}

\item{plot_type}{Character. For Poisson: "standard" or "compensated". Ignored for other processes.}

\item{alpha}{Numeric. Transparency level for path lines (0 to 1). Default is 0.3.}

\item{theme}{Character. ggplot2 theme to use. One of "minimal", "classic", "bw". Default is "minimal".}
}
\value{
A ggplot object.
}
\description{
Creates professional visualizations of simulated stochastic process paths
using ggplot2. Automatically detects the process type and applies appropriate
styling.
}
\details{
The function automatically detects the process type from the paths_data attributes
and applies appropriate labels and styling. For GBM processes, it includes the
SDE equation in the subtitle. For Poisson processes, use \code{plot_type} to choose
between standard or compensated processes.
}
\examples{
# Simulate and plot GBM
gbm_spec(100, 0.05, 0.2) |>
  simulate_paths(n_paths = 50, n_steps = 252, maturity = 1) |>
  plot_paths(n_paths_plot = 10)

# Simulate and plot Poisson
poisson_spec(1.0) |>
  simulate_paths(25, 500, 30) |>
  plot_paths(plot_type = "compensated")

}
