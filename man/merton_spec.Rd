% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merton_model.R
\name{merton_spec}
\alias{merton_spec}
\title{Create Merton Jump-Diffusion Specification}
\usage{
merton_spec(
  initial_price,
  risk_free_rate,
  volatility,
  jump_intensity,
  jump_mean,
  jump_sd
)
}
\arguments{
\item{initial_price}{Numeric. Initial asset price \eqn{S_0 > 0}.}

\item{risk_free_rate}{Numeric. Continuously compounded risk-free rate \eqn{r}.}

\item{volatility}{Numeric. Diffusive volatility \eqn{\sigma > 0}.}

\item{jump_intensity}{Numeric. Jump arrival intensity \eqn{\lambda > 0}.}

\item{jump_mean}{Numeric. Mean of the logarithmic jump size \eqn{\mu_J}.}

\item{jump_sd}{Numeric. Standard deviation of the logarithmic jump size \eqn{\sigma_J > 0}.}
}
\value{
An object of class \code{merton_spec} inheriting from \code{process_spec}.
}
\description{
Defines the parameters for the Merton (1976) jump-diffusion model where the
logarithmic price follows a diffusion with normally distributed jumps.
}
\examples{
spec <- merton_spec(
  initial_price = 100,
  risk_free_rate = 0.03,
  volatility = 0.2,
  jump_intensity = 1.0,
  jump_mean = -0.1,
  jump_sd = 0.2
)

spec |>
  simulate_paths(n_paths = 100, n_steps = 252, maturity = 1)

}
