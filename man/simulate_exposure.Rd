% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_risk.R
\name{simulate_exposure}
\alias{simulate_exposure}
\title{Simulate Portfolio Exposure}
\usage{
simulate_exposure(spec, process_spec, ...)
}
\arguments{
\item{spec}{Exposure specification created by \code{\link[=exposure_spec]{exposure_spec()}}.}

\item{process_spec}{Short-rate specification produced by \code{\link[=short_rate_spec]{short_rate_spec()}}.}

\item{n_paths}{Integer. Number of Monte Carlo paths.}

\item{n_steps}{Integer. Number of time steps.}

\item{maturity}{Numeric. Horizon in years. Defaults to the maximum payment
date across all trades.}

\item{seed}{Integer seed passed to \code{\link[=simulate_paths]{simulate_paths()}}.}

\item{keep_paths}{Logical. When \code{TRUE}, path-level exposures are included in
the result for downstream VaR calculations.}
}
\value{
An object of class \code{portfolio_exposure_result} containing portfolio,
netting-set, and trade summaries plus optional path-level detail.
}
\description{
Run Monte Carlo exposure analytics for a portfolio created via
\code{\link[=exposure_spec]{exposure_spec()}}. The engine simulates short-rate paths once and revalues all
trades per path, producing tidy summaries for trades, netting sets, and the
aggregate portfolio. Netting aggregation follows the lecture workflow in
\code{Exposures_HW_Netting.py} by applying positive-part logic \emph{after} summing
trades within each set.
}
