% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlated_bm_process.R
\name{correlated_bm_spec}
\alias{correlated_bm_spec}
\title{Create Correlated Brownian Motion Specification}
\usage{
correlated_bm_spec(
  initial_values,
  drift,
  covariance_matrix,
  component_names = NULL
)
}
\arguments{
\item{initial_values}{Numeric vector. Initial values for each component.}

\item{drift}{Numeric vector. Drift parameters \eqn{\boldsymbol{\mu}} for each
component (annualised).}

\item{covariance_matrix}{Numeric matrix. Positive semi-definite covariance
matrix \eqn{\Sigma} describing instantaneous covariances.}

\item{component_names}{Character vector. Optional names for the components.
Defaults to \code{paste0("component_", seq_along(initial_values))} when \code{NULL}.}
}
\value{
An object of class \code{correlated_bm_spec} inheriting from
\code{process_spec}.
}
\description{
Defines a multivariate Brownian motion with drift and covariance structure
suitable for simulating correlated asset paths. The specification follows the
tidymodels/hardhat design pattern and integrates with \code{simulate_paths()}.
}
\details{
\subsection{Mathematical Formulation}{

A correlated Brownian motion \eqn{\mathbf{X}(t)} with drift satisfies
\deqn{d\mathbf{X}(t) = \boldsymbol{\mu}\, dt + L\, d\mathbf{W}(t)}
where \eqn{L} is a Cholesky factor of \eqn{\Sigma} (i.e. \eqn{LL^\top = \Sigma})
and \eqn{\mathbf{W}(t)} is a vector of independent Brownian motions.
}
}
\examples{
spec <- correlated_bm_spec(
  initial_values = c(100, 95),
  drift = c(0.05, 0.04),
  covariance_matrix = matrix(c(0.04, 0.03, 0.03, 0.09), nrow = 2),
  component_names = c("Asset_A", "Asset_B")
)

spec |>
  simulate_paths(n_paths = 100, n_steps = 252, maturity = 1) |>
  plot_paths()

}
