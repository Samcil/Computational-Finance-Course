% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/short_rate_spec.R
\name{short_rate_spec}
\alias{short_rate_spec}
\title{Short-Rate Model Specification}
\usage{
short_rate_spec(
  model = c("ho_lee", "hull_white", "g2pp"),
  volatility,
  mean_reversion = NULL,
  second_volatility = NULL,
  second_mean_reversion = NULL,
  correlation = 0,
  curve = NULL,
  initial_rate = NULL,
  engine = "monte_carlo",
  engine_options = list()
)
}
\arguments{
\item{model}{Character. One of "ho_lee" or "hull_white".}

\item{volatility}{Numeric. Instantaneous volatility of the short rate.}

\item{mean_reversion}{Numeric. Mean-reversion speed (required for Hull-White,
ignored for Ho-Lee).}

\item{curve}{Either a \code{term_structure_fit} produced by \code{\link[=fit]{fit()}}, or a data
frame/tibble with columns \code{tenor} (in years) and \code{discount_factor}.}

\item{initial_rate}{Optional numeric override for the initial short rate. If
omitted, the rate is inferred from the supplied discount curve via the
instantaneous forward rate at time zero.}

\item{engine}{Character identifier for the simulation engine. Defaults to
"monte_carlo".}

\item{engine_options}{Named list of engine-specific options.}
}
\value{
An object of class \code{short_rate_spec} (inheriting from
\code{model_spec}) that can be passed to \code{\link[=simulate_paths]{simulate_paths()}}.
}
\description{
Construct a specification for one-factor Gaussian short-rate models with a
focus on the Ho-Lee setup. The specification stores the volatility parameter
together with a discount curve that is used to infer the instantaneous
forward and drift adjustments required for arbitrage-free simulations.
}
\examples{
curve <- tibble::tibble(
  tenor = c(0, 1, 2, 3),
  discount_factor = exp(-0.02 * tenor)
)

spec <- short_rate_spec(
  model = "ho_lee",
  volatility = 0.01,
  curve = curve
)

}
