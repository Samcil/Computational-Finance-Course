% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_risk.R
\name{exposure_spec}
\alias{exposure_spec}
\title{Exposure Specification for Interest-Rate Portfolios}
\usage{
exposure_spec(trades, quantiles = 0.95, discount = TRUE)
}
\arguments{
\item{trades}{List of trades. Each trade must be a list containing
\code{schedule} (output of \code{\link[=swap_cashflow_schedule]{swap_cashflow_schedule()}}), \code{fixed_rate}, \code{type}
(\code{"payer"} or \code{"receiver"}), optional \code{trade_id}, and optional
\code{netting_set} (defaults to the trade identifier).}

\item{quantiles}{Numeric vector of tail probabilities in \verb{(0, 1)} at which
potential future exposure (PFE) is reported.}

\item{discount}{Logical. When \code{TRUE} (default) the summary reports discounted
positive exposure in addition to undiscounted metrics.}
}
\value{
An object of class \code{exposure_spec} that can be passed to
\code{\link[=simulate_exposure]{simulate_exposure()}}.
}
\description{
Assemble a portfolio of interest-rate swaps and capture netting set
assignments together with reporting preferences for exposure analytics. The
resulting specification plugs into \code{\link[=simulate_exposure]{simulate_exposure()}} which reuses the
short-rate Monte Carlo engines to produce swap-level, netting-set, and
portfolio summaries in line with the lecture scripts
\code{Exposures_HW_Netting.py} and \code{ConvexityCorrection.py}.
}
