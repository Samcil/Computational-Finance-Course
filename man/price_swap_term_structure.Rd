% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term_structure_sensitivities.R
\name{price_swap_term_structure}
\alias{price_swap_term_structure}
\title{Price a Swap Using a Calibrated Term Structure}
\usage{
price_swap_term_structure(
  fit,
  schedule,
  fixed_rate,
  type = c("payer", "receiver"),
  discount_curve = NULL,
  projection_curve = NULL
)
}
\arguments{
\item{fit}{A \code{term_structure_fit} resulting from \code{\link[=fit]{fit()}}.}

\item{schedule}{Tibble produced by \code{\link[=swap_cashflow_schedule]{swap_cashflow_schedule()}} containing the
swap payment dates.}

\item{fixed_rate}{Numeric. Fixed coupon rate expressed in decimal form.}

\item{type}{Character. Either "payer" (pay fixed, receive float) or
"receiver" (receive fixed, pay float).}

\item{discount_curve}{Optional identifier selecting the discount curve used
for cash-flow present value calculations. Required when multiple curves
are present in the calibration.}

\item{projection_curve}{Optional identifier selecting the projection curve
used to derive forward rates. Defaults to \code{discount_curve} when omitted.}
}
\value{
Tibble with metrics \code{pv} and \code{dv01}.
}
\description{
Evaluate the present value and DV01 of a fixed-for-floating interest rate
swap using discount factors implied by a \code{term_structure_fit}.
}
\examples{
spec <- term_structure_spec()
quotes <- tibble::tibble(tenor = c(0.5, 1, 2), quote = c(0.02, 0.021, 0.024))
fit_obj <- fit(spec, quotes)
schedule <- swap_cashflow_schedule(0, 2, frequency = 2, notional = 1e6)
price_swap_term_structure(fit_obj, schedule, fixed_rate = 0.025)

}
