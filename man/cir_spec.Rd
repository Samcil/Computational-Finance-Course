% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cir_process.R
\name{cir_spec}
\alias{cir_spec}
\title{Create CIR Process Specification}
\usage{
cir_spec(
  initial_value,
  mean_reversion,
  long_term_mean,
  volatility,
  engine = "euler",
  engine_options = list()
)
}
\arguments{
\item{initial_value}{Numeric. Initial variance/rate \eqn{v_0}.}

\item{mean_reversion}{Numeric. Mean reversion speed \eqn{\kappa > 0}.}

\item{long_term_mean}{Numeric. Long-term equilibrium level \eqn{\theta > 0}.}

\item{volatility}{Numeric. Volatility of volatility \eqn{\sigma > 0}.}

\item{engine}{Character identifier for the simulation engine. Defaults to
"euler".}

\item{engine_options}{Named list of engine-specific options passed to
\code{\link[=set_engine]{set_engine()}}. Reserved for future use.}
}
\value{
A cir_spec object containing the process parameters.
}
\description{
Creates a specification for the Cox-Ingersoll-Ross (CIR) mean-reverting
square-root diffusion process.
}
\details{
\subsection{Mathematical Formulation}{

The CIR process follows:
\deqn{dv(t) = \kappa(\theta - v(t))dt + \sigma\sqrt{v(t)}dW(t)}

Where:
\itemize{
\item \eqn{\kappa}: Mean reversion speed
\item \eqn{\theta}: Long-term mean (equilibrium)
\item \eqn{\sigma}: Volatility of volatility
\item \eqn{v(t)}: Variance/rate at time t
}

\strong{Feller Condition}: \eqn{2\kappa\theta \geq \sigma^2} ensures \eqn{v(t) > 0}.
}
}
\examples{
# Interest rate model
spec <- cir_spec(0.05, 3.0, 0.04, 0.2)

# Pipeline workflow
cir_spec(0.05, 3.0, 0.04, 0.2) |>
  simulate_paths(50, 252, 1.0) |>
  plot_paths()

}
