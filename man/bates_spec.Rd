% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bates_model.R
\name{bates_spec}
\alias{bates_spec}
\title{Create Bates Model Specification}
\usage{
bates_spec(
  initial_price,
  initial_variance,
  risk_free_rate,
  dividend_yield = 0,
  mean_reversion,
  long_term_variance,
  vol_of_vol,
  correlation,
  jump_intensity,
  jump_mean,
  jump_sd,
  scheme = c("aes", "euler")
)
}
\arguments{
\item{initial_price}{Numeric. Initial asset price \eqn{S_0 > 0}.}

\item{initial_variance}{Numeric. Initial variance \eqn{v_0 \ge 0}.}

\item{risk_free_rate}{Numeric. Continuously compounded risk-free rate \eqn{r}.}

\item{dividend_yield}{Numeric. Continuous dividend yield \eqn{q}. Default is 0.}

\item{mean_reversion}{Numeric. Variance mean reversion speed \eqn{\kappa > 0}.}

\item{long_term_variance}{Numeric. Long-run variance level \eqn{\theta > 0}.}

\item{vol_of_vol}{Numeric. Volatility of variance \eqn{\xi > 0}.}

\item{correlation}{Numeric. Instantaneous correlation \eqn{\rho \in [-1, 1]}.}

\item{jump_intensity}{Numeric. Jump intensity \eqn{\lambda \ge 0}.}

\item{jump_mean}{Numeric. Mean of log jump sizes \eqn{\mu_J}.}

\item{jump_sd}{Numeric. Standard deviation of log jump sizes \eqn{\sigma_J > 0}.}

\item{scheme}{Character. Simulation scheme, one of "aes" (default) or "euler".}
}
\value{
An object of class \code{bates_spec} inheriting from \code{process_spec}.
}
\description{
Defines the Bates (1996) stochastic volatility model with jumps, combining
Heston variance dynamics with Merton-style normally distributed jumps. The
specification integrates with \code{simulate_paths()} and helper utilities to
generate paths, evaluate characteristic functions, and recover Black-Scholes
implied volatilities.
}
\examples{
spec <- bates_spec(
  initial_price = 100,
  initial_variance = 0.04,
  risk_free_rate = 0.02,
  dividend_yield = 0.01,
  mean_reversion = 1.5,
  long_term_variance = 0.04,
  vol_of_vol = 0.5,
  correlation = -0.6,
  jump_intensity = 0.8,
  jump_mean = -0.05,
  jump_sd = 0.2
)

spec |>
  simulate_paths(n_paths = 50, n_steps = 200, maturity = 1)

}
