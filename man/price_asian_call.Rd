% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asian_options.R
\name{price_asian_call}
\alias{price_asian_call}
\alias{price_asian_put}
\title{Asian Option Pricing via Monte Carlo}
\usage{
price_asian_call(
  process_spec,
  strike,
  maturity,
  n_paths = 1000L,
  n_steps = 252L,
  seed = NULL,
  method = c("standard", "antithetic")
)

price_asian_put(
  process_spec,
  strike,
  maturity,
  n_paths = 10000L,
  n_steps = 252L,
  seed = NULL,
  method = c("standard", "antithetic")
)
}
\arguments{
\item{process_spec}{A \code{gbm_spec} object describing the underlying asset
dynamics under the risk-neutral measure.}

\item{strike}{Numeric vector of strike prices.}

\item{maturity}{Numeric. Time to maturity in years.}

\item{n_paths}{Integer. Number of Monte Carlo paths to simulate. Default is
1000.}

\item{n_steps}{Integer. Number of monitoring points along each path. Default
is 252.}

\item{seed}{Optional integer seed for reproducibility. When supplied, the
same seed is used for standard and antithetic simulations.}

\item{method}{Character. Sampling approach, either "standard" Monte Carlo or
"antithetic" variates. Default is "standard".}
}
\value{
A tibble with columns \code{strike}, \code{option_type}, \code{method}, \code{price},
\code{std_error}, \code{n_paths}, \code{n_steps}, and \code{maturity}.
}
\description{
Prices arithmetic-average Asian options using Monte Carlo simulation under a
Geometric Brownian Motion (GBM) model. Supports standard sampling and
antithetic variates for variance reduction.
}
\examples{
spec <- gbm_spec(initial_value = 100, drift = 0.03, volatility = 0.2)
price_asian_call(spec, strike = c(95, 105), maturity = 1, n_paths = 2000,
                 n_steps = 126, seed = 42)

}
