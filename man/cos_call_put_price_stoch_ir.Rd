% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cos_method.R
\name{cos_call_put_price_stoch_ir}
\alias{cos_call_put_price_stoch_ir}
\title{COS Pricing with Stochastic Short Rates}
\usage{
cos_call_put_price_stoch_ir(
  cf,
  option_type = c("call", "put"),
  spot,
  maturity,
  strikes,
  discount_factor,
  n_terms = 256L,
  truncation = 8,
  lower_bound = NULL,
  upper_bound = NULL
)
}
\arguments{
\item{cf}{Function returning the characteristic function evaluated at complex
arguments. The function must be vectorised over the input \code{u} and return a
complex vector.}

\item{option_type}{Character string, either "call" or "put".}

\item{spot}{Numeric scalar. Current underlying price.}

\item{maturity}{Numeric scalar. Time to maturity in years.}

\item{strikes}{Numeric vector of strike prices.}

\item{discount_factor}{Numeric scalar or vector giving the discount factor
\eqn{P(0, T)} for each maturity in \code{maturity}. Must either have length 1 or
match the length of \code{strikes} when multiple maturities are priced.}

\item{n_terms}{Integer. Number of COS expansion terms. Default is 256.}

\item{truncation}{Numeric scalar controlling the integration domain size.
Default is 8. Ignored when \code{lower_bound} and \code{upper_bound} are supplied.}

\item{lower_bound}{Optional numeric scalar specifying the lower limit of the
truncated integration domain.}

\item{upper_bound}{Optional numeric scalar specifying the upper limit of the
truncated integration domain.}
}
\value{
Tibble with columns \code{strike} and \code{price} containing COS prices.
}
\description{
Applies the COS expansion to models in which interest rates are stochastic
and discounted via an externally supplied zero-coupon bond price. Mirrors
the lecture implementations used for Black-Scholes-Hull-White and related
hybrids where characteristic functions are defined under the terminal
measure.
}
