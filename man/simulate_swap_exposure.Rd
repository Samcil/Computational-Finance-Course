% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swap_exposure.R
\name{simulate_swap_exposure}
\alias{simulate_swap_exposure}
\title{Simulate Interest-Rate Swap Exposure Profiles}
\usage{
simulate_swap_exposure(
  spec,
  schedule,
  fixed_rate,
  type = c("payer", "receiver"),
  n_paths,
  n_steps,
  maturity = max(schedule[["pay_time"]]),
  quantiles = 0.95,
  seed = 123,
  keep_paths = FALSE
)
}
\arguments{
\item{spec}{A \code{\link{short_rate_spec}} constructed via \code{\link[=short_rate_spec]{short_rate_spec()}}.}

\item{schedule}{Tibble produced by \code{\link[=swap_cashflow_schedule]{swap_cashflow_schedule()}} describing the
swap cash-flow schedule.}

\item{fixed_rate}{Numeric scalar giving the fixed leg coupon in decimal form.}

\item{type}{Character string indicating the swap direction. Either
\code{"payer"} (pay fixed, receive float) or \code{"receiver"}.}

\item{n_paths}{Integer. Number of Monte Carlo paths.}

\item{n_steps}{Integer. Number of simulation steps over the horizon.}

\item{maturity}{Numeric scalar. Simulation horizon in years. Defaults to the
maximum payment time in \code{schedule}.}

\item{quantiles}{Numeric vector of probabilities in \verb{(0, 1)} at which PFEs
are reported. Defaults to \code{0.95}.}

\item{seed}{Integer seed for reproducibility.}

\item{keep_paths}{Logical. When \code{TRUE}, the returned list contains the full
path-level exposures in addition to the aggregated summary.}
}
\value{
A list with elements:
\itemize{
\item \code{summary}: tibble containing expected exposure (undiscounted), expected
discounted exposure, and PFE columns for each requested quantile.
\item \code{paths}: tibble of pathwise results (present when \code{keep_paths = TRUE}) with
columns \code{path_id}, \code{time}, \code{short_rate}, \code{discount_factor},
\code{swap_value}, \code{positive_exposure}, and \code{discounted_positive_exposure}.
}
}
\description{
Generate Monte Carlo exposure profiles for a fixed-for-floating interest rate
swap under a Gaussian short-rate specification. The routine evaluates pathwise
swap values, positive exposures, and discounted exposures that are suitable
for CVA-style analytics while returning tidy summaries of expected exposure
(EE) and potential future exposure (PFE) at supplied confidence levels.
}
\examples{
curve <- tibble::tibble(tenor = 0:5, discount_factor = exp(-0.02 * tenor))
spec <- short_rate_spec(model = "ho_lee", volatility = 0.01, curve = curve)
schedule <- swap_cashflow_schedule(start = 0, end = 5, frequency = 2, notional = 1e6)
exposures <- simulate_swap_exposure(
  spec = spec,
  schedule = schedule,
  fixed_rate = 0.02,
  type = "payer",
  n_paths = 1000,
  n_steps = 200,
  quantiles = c(0.9, 0.95)
)

}
