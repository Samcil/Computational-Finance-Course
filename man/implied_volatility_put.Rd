% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/implied_volatility.R
\name{implied_volatility_put}
\alias{implied_volatility_put}
\title{Compute Black-Scholes Implied Volatility for Puts}
\usage{
implied_volatility_put(
  option_data,
  spot,
  maturity,
  risk_free_rate,
  dividend_yield = 0,
  lower = 1e-04,
  upper = 3,
  tol = 1e-06,
  max_iter = 100
)
}
\arguments{
\item{option_data}{A data frame containing at least the columns \code{strike} and
\code{price} representing observed call prices.}

\item{spot}{Numeric scalar spot price \eqn{S_0 > 0}.}

\item{maturity}{Numeric time to maturity in years.}

\item{risk_free_rate}{Numeric continuously compounded risk-free rate.}

\item{dividend_yield}{Numeric continuous dividend yield. Default is 0.}

\item{lower}{Numeric lower bound for the implied volatility search interval.
Default is \code{1e-4}.}

\item{upper}{Numeric upper bound for the implied volatility search interval.
Default is \code{3}.}

\item{tol}{Numeric tolerance supplied to \code{uniroot()}. Default is \code{1e-6}.}

\item{max_iter}{Integer maximum number of iterations for \code{uniroot()}.}
}
\value{
A tibble with the original columns plus \code{implied_volatility}.
}
\description{
Estimates implied volatilities for European put options by inverting the
Black-Scholes pricing formula.
}
\examples{
strikes <- c(90, 100, 110)
true_vols <- c(0.18, 0.2, 0.22)
prices <- purrr::map2_dbl(
  strikes,
  true_vols,
  ~ price_options(
      black_scholes_spec(
        option_type = "put",
        strike = .x,
        maturity = 1,
        risk_free_rate = 0.02
      ),
      spot = 100,
      volatility = .y
    )$price
)

option_tbl <- tibble::tibble(strike = strikes, price = prices)
implied_volatility_put(
  option_tbl,
  spot = 100,
  maturity = 1,
  risk_free_rate = 0.02
)

}
