% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barrier_options.R
\name{price_barrier_option}
\alias{price_barrier_option}
\title{Barrier Option Pricing via Monte Carlo}
\usage{
price_barrier_option(
  process_spec,
  strike,
  barrier,
  maturity,
  barrier_type = c("up-and-out", "up-and-in", "down-and-out", "down-and-in"),
  option_type = c("call", "put"),
  n_paths = 1000L,
  n_steps = 252L,
  seed = NULL
)
}
\arguments{
\item{process_spec}{A \code{gbm_spec} object describing the underlying asset
dynamics under the risk-neutral measure.}

\item{strike}{Numeric vector of strike prices.}

\item{barrier}{Numeric vector of barrier levels. When both \code{strike} and
\code{barrier} have length greater than one, all combinations are evaluated.}

\item{maturity}{Numeric. Time to maturity in years.}

\item{barrier_type}{Character. One of "up-and-out", "up-and-in",
"down-and-out", or "down-and-in".}

\item{option_type}{Character. Option payoff, either "call" or "put".}

\item{n_paths}{Integer. Number of Monte Carlo paths. Default is 1000.}

\item{n_steps}{Integer. Number of monitoring points along each path. Default
is 252.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A tibble containing \code{strike}, \code{barrier}, \code{option_type},
\code{barrier_type}, \code{price}, \code{std_error}, \code{hit_probability}, \code{n_paths}, and
\code{n_steps}.
}
\description{
Prices single-asset barrier options under a Geometric Brownian Motion model
using Monte Carlo simulation. Supports up/down and in/out structures for call
and put payoffs.
}
\examples{
spec <- gbm_spec(initial_value = 100, drift = 0.02, volatility = 0.2)
price_barrier_option(
  process_spec = spec,
  strike = 100,
  barrier = 120,
  maturity = 1,
  barrier_type = "up-and-out",
  option_type = "call",
  n_paths = 5000,
  n_steps = 200,
  seed = 123
)

}
