% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortgage_products.R
\name{mortgage_annuity_spec}
\alias{mortgage_annuity_spec}
\title{Mortgage Annuity Specification}
\usage{
mortgage_annuity_spec(
  principal,
  coupon_rate,
  maturity,
  frequency = 12,
  daycount = NULL,
  payment = NULL,
  prepayment = 0,
  engine = "deterministic",
  engine_options = list()
)
}
\arguments{
\item{principal}{Numeric scalar describing the initial mortgage balance.}

\item{coupon_rate}{Numeric scalar giving the nominal annual mortgage rate.}

\item{maturity}{Numeric scalar for the mortgage maturity in years.}

\item{frequency}{Integer number of payments per year (for example \code{12} for
monthly).}

\item{daycount}{Optional numeric value for the accrual fraction per period.
Defaults to \code{1 / frequency} when omitted.}

\item{payment}{Optional numeric override for the level payment each period.
When \code{NULL} the payment is solved to exactly amortise the mortgage.}

\item{prepayment}{Optional numeric scalar or vector describing the constant
or time-varying conditional prepayment rate (CPR). Accepts a scalar (used
for every period), a vector of length equal to the number of periods, or a
vector of length \code{periods + 1} mirroring the Python reference
implementation where the first element corresponds to time zero.}

\item{engine}{Character scalar identifying the engine. Currently only
\code{'deterministic'} is supported.}

\item{engine_options}{Named list of engine-specific options. Reserved for
future extensions.}
}
\value{
Object of class \code{mortgage_annuity_spec} inheriting from \code{model_spec}.
}
\description{
Create a specification wrapping the annuity mortgage schedule so that pricing
and swap utilities can consume a consistent contract object. The schedule is
stored on the specification for reuse across valuation and simulation
routines.
}
