% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term_structure_spec.R
\name{term_structure_spec}
\alias{term_structure_spec}
\title{Term Structure Specification}
\usage{
term_structure_spec(
  curve_type = c("ois", "libor"),
  quote_type = c("zero_rate", "discount_factor"),
  engine = "direct",
  engine_options = list()
)
}
\arguments{
\item{curve_type}{Character. The underlying curve being calibrated. Options
are \code{"ois"} for overnight indexed swap discounting or \code{"libor"} for
forward rate curves. Defaults to \code{"ois"}.}

\item{quote_type}{Character. Market quotes supplied in the calibration data.
Supported values are \code{"zero_rate"} (continuously compounded) and
\code{"discount_factor"}. Defaults to \code{"zero_rate"}.}

\item{engine}{Character. Engine identifier. Supported values are
\code{"direct"} (closed-form conversion of zero/discount quotes),
\code{"bootstrap"} (piecewise construction from a mix of zero/discount and par
swap quotes), \code{"newton"} (nonlinear solver matching instrument
residuals), \code{"multi_curve_newton"} (joint discount/projection curve
calibration), and \code{"treasury_bootstrap"} (coupon bond bootstrapping for
treasury securities).}

\item{engine_options}{Named list with engine-specific configuration. Common
entries include \code{instrument_col} (defaults to "instrument"),
\code{fixed_leg_frequency} (number of fixed coupons per year, defaults to 1),
and \code{max_iter} (maximum iterations for the Newton engine).}
}
\value{
A \code{term_structure_spec} object that can be passed to
\code{\link[=fit]{fit()}} along with market quotes.
}
\description{
Construct a specification for deterministic term-structure calibration that
follows tidymodels and hardhat design conventions. The specification stores
curve-level metadata, expected quote types, and engine settings used during
calibration.
}
\examples{
spec <- term_structure_spec()
spec

}
