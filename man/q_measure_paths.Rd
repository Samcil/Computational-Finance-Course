% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure_switching.R
\name{q_measure_paths}
\alias{q_measure_paths}
\title{Measure Switching Utilities for Diffusions}
\usage{
q_measure_paths(
  spec,
  risk_free_rate,
  n_paths,
  n_steps,
  maturity,
  dividend_yield = 0,
  market_price_of_risk = NULL,
  seed = 123
)
}
\arguments{
\item{spec}{A \code{gbm_spec} representing dynamics under the physical measure.}

\item{risk_free_rate}{Numeric risk-free rate used for the risk-neutral
measure.}

\item{n_paths}{Integer number of Monte Carlo paths.}

\item{n_steps}{Integer number of Euler steps.}

\item{maturity}{Numeric time horizon in years.}

\item{dividend_yield}{Optional continuously compounded dividend yield. Must
be scalar. Defaults to \code{0}.}

\item{market_price_of_risk}{Optional scalar market price of risk (lambda).
When \code{NULL} it is implied from the specification drift via
\code{(mu - (r - q)) / sigma}.}

\item{seed}{Integer seed controlling the shared random numbers.}
}
\value{
Tibble with columns \code{measure}, \code{path_id}, \code{time}, \code{stock_price}, and
\code{radon_nikodym}. Rows tagged with \code{measure = "P"} include the Radon--Nikodym
derivative \code{dQ/dP} for the corresponding path and time. The tibble inherits
the S3 class \code{measure_paths} and stores attributes \code{market_price_of_risk},
\code{risk_free_rate}, and \code{dividend_yield}.
}
\description{
Provides helpers to simulate paths under the physical (P) and risk-neutral
(Q) measures using common random numbers together with Radon--Nikodym
densities for reweighting. The implementation is aligned with the
\code{PathsUnderQandPmeasure.py} lecture script.
}
\examples{
spec <- gbm_spec(initial_value = 100, drift = 0.08, volatility = 0.2)
paths <- q_measure_paths(
  spec = spec,
  risk_free_rate = 0.03,
  n_paths = 32,
  n_steps = 12,
  maturity = 1,
  dividend_yield = 0.01,
  seed = 123
)
dplyr::filter(paths, time == 1)

}
