% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term_structure_sensitivities.R
\name{term_structure_quote_sensitivities}
\alias{term_structure_quote_sensitivities}
\title{Quote-Level Sensitivities via Bump-and-Revalue}
\usage{
term_structure_quote_sensitivities(
  spec,
  market_data,
  valuation_fun,
  bump_size = 1e-04,
  bump_type = c("additive", "relative"),
  bump_column = "quote"
)
}
\arguments{
\item{spec}{A \code{term_structure_spec} configured with the desired calibration
engine.}

\item{market_data}{Tidy data frame of market quotes supplied to \code{\link[=fit]{fit()}}.}

\item{valuation_fun}{Function accepting a \code{term_structure_fit} and returning
a numeric scalar valuation (for example swap PV).}

\item{bump_size}{Numeric bump applied to each quote. Defaults to \code{1e-4}.}

\item{bump_type}{Either "additive" (default) or "relative". Relative bumps
scale \code{bump_size} by the absolute quote magnitude, falling back to
\code{bump_size} when the quote is zero.}

\item{bump_column}{Column within \code{market_data} that is perturbed. Defaults to
"quote".}
}
\value{
Tibble containing the original quote metadata alongside the bumped
valuation and sensitivity.
}
\description{
Compute finite-difference sensitivities of a valuation functional with
respect to the market quotes used in term-structure calibration. The
procedure mirrors the Python \code{YieldCurveBuildGreeks.py} workflow by
recalibrating the curve after bumping each quote individually.
}
