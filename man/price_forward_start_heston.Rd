% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forward_start_options.R
\name{price_forward_start_heston}
\alias{price_forward_start_heston}
\title{Price Forward Start Options under Heston}
\usage{
price_forward_start_heston(
  process_spec,
  start,
  maturity,
  strike_adjustment = 0,
  option_type = c("call", "put"),
  n_terms = 256L,
  truncation = 10
)
}
\arguments{
\item{process_spec}{A \code{heston_spec} object describing the model parameters.}

\item{start}{Numeric. Forward-start time \eqn{T_1} in years.}

\item{maturity}{Numeric. Final maturity \eqn{T_2} in years.}

\item{strike_adjustment}{Numeric vector of strike adjustments \eqn{k}. The
effective strike multiple is \code{1 + strike_adjustment}.}

\item{option_type}{Character. One of "call" or "put".}

\item{n_terms}{Integer. Number of COS expansion terms. Default is 256.}

\item{truncation}{Numeric. Truncation domain size controlling the COS
integration interval. Default is 10.}
}
\value{
A tibble with columns \code{strike_adjustment}, \code{strike_multiplier}, and
\code{price} (time-zero option value).
}
\description{
Prices forward start options where the strike is determined at time \code{start}
as \eqn{K = (1 + k) S(T_1)} and the payoff at \code{maturity} is evaluated under
Heston dynamics. Pricing is performed via the COS method applied to the
log-return \eqn{\log(S(T_2) / S(T_1))} using the characteristic function
produced by \code{\link[=forward_start_characteristic_function]{forward_start_characteristic_function()}}.
}
\seealso{
\code{\link[=forward_start_characteristic_function]{forward_start_characteristic_function()}}, \code{\link[=cos_call_put_price]{cos_call_put_price()}}
}
