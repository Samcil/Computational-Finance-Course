% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure_switching.R
\name{short_rate_measure_paths}
\alias{short_rate_measure_paths}
\title{Measure Switching Utilities for Short-Rate Models}
\usage{
short_rate_measure_paths(
  spec,
  n_paths,
  n_steps,
  maturity,
  market_price_of_risk = 0,
  seed = 123
)
}
\arguments{
\item{spec}{A \code{short_rate_spec} created by \code{\link[=short_rate_spec]{short_rate_spec()}}.}

\item{n_paths}{Integer number of Monte Carlo paths.}

\item{n_steps}{Integer number of time steps.}

\item{maturity}{Numeric time horizon in years.}

\item{market_price_of_risk}{Market price of risk input controlling the
Girsanov drift adjustment. Accepts a numeric scalar, numeric vector of
length \code{n_steps}, or a function of time returning numeric values. Defaults
to \code{0}, implying that P and Q coincide.}

\item{seed}{Integer seed for reproducibility.}
}
\value{
Tibble with columns \code{measure}, \code{path_id}, \code{time}, \code{short_rate},
\code{discount_factor}, and \code{radon_nikodym}. Rows labelled \code{measure = "P"}
contain the Radon--Nikodym derivative \code{dQ/dP}. The tibble carries the class
\code{short_rate_measure_paths} in addition to \code{measure_paths}.
}
\description{
Generates parallel simulations of Gaussian short-rate models under the
physical (P) and risk-neutral (Q) measures using a shared Brownian driver.
The routine returns Radon--Nikodym weights so that expectations computed
under the physical measure can be reweighted to match risk-neutral pricing.
}
\examples{
curve <- tibble::tibble(tenor = c(0, 1, 2), discount_factor = exp(-0.03 * tenor))
spec <- short_rate_spec(model = "ho_lee", volatility = 0.01, curve = curve)
paths <- short_rate_measure_paths(
  spec = spec,
  n_paths = 1000,
  n_steps = 120,
  maturity = 1,
  market_price_of_risk = 0.3,
  seed = 42
)
dplyr::filter(paths, time == 1, measure == "P")

}
