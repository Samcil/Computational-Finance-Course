% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term_structure_sensitivities.R
\name{term_structure_discount_function}
\alias{term_structure_discount_function}
\title{Build a Discount Function from a Term Structure Fit}
\usage{
term_structure_discount_function(
  fit,
  curve = NULL,
  method = c("spline", "linear")
)
}
\arguments{
\item{fit}{A \code{term_structure_fit} produced by \code{\link[=fit]{fit()}}.}

\item{curve}{Optional character identifier selecting the curve to extract
when the calibration contains multiple curves.}

\item{method}{Interpolation scheme. Either "spline" (default) for a natural
cubic spline in log-discount space or "linear" for piecewise-linear
interpolation of discount factors.}
}
\value{
A function accepting numeric maturities (in years) and returning
discount factors.
}
\description{
Construct a discount factor interpolation from a calibrated
\code{term_structure_fit}. The resulting function can be supplied to pricing
helpers that expect a continuous discount curve.
}
\examples{
spec <- term_structure_spec()
quotes <- tibble::tibble(tenor = c(0.5, 1, 2), quote = c(0.02, 0.021, 0.024))
fit_obj <- fit(spec, quotes)
df_fun <- term_structure_discount_function(fit_obj)
df_fun(c(0.5, 1))

}
