% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swap_pricing.R
\name{price_swap}
\alias{price_swap}
\title{Price a Fixed-for-Floating Interest Rate Swap}
\usage{
price_swap(spec, schedule, fixed_rate, type = c("payer", "receiver"))
}
\arguments{
\item{spec}{A \code{short_rate_spec} created by \code{\link[=short_rate_spec]{short_rate_spec()}}.}

\item{schedule}{Tibble produced by \code{\link[=swap_cashflow_schedule]{swap_cashflow_schedule()}} containing the
swap payment dates.}

\item{fixed_rate}{Numeric. Fixed coupon rate expressed in decimal form.}

\item{type}{Character. Either "payer" (pay fixed, receive float) or
"receiver" (receive fixed, pay float).}
}
\value{
A tibble with metrics \code{pv} and \code{dv01}.
}
\description{
Compute the present value and DV01 of a plain vanilla interest rate swap
under a supplied short-rate specification using discount factors from the
calibrated curve. Positive PV indicates value to the receiver of the fixed
leg.
}
\examples{
curve <- tibble::tibble(tenor = 0:5, discount_factor = exp(-0.03 * tenor))
spec <- short_rate_spec(volatility = 0.0, curve = curve)
schedule <- swap_cashflow_schedule(0, 5, frequency = 1, notional = 1e6)
price_swap(spec, schedule, fixed_rate = 0.03)

}
